function delDonationClasses() {
	var count = 0;
	if (count == 0) {
		alert("삭제할 ")
	}
	alert(document.mypagefrm.delete_cb.length);
	/*if(document.mypagefrm.delete_cb.length)
	alert(document.mypagefrm.delete_cb.checked);
//	if(document.delete_cb.length)
	
	
	
	
	var count = 0;
	// 하나 체크했을때의 경우
	if (document.formm.cseq.length == undefined) { // 배열은 length가 측정되는데 그냥 하나의 객체는 length 측정이 불가능하기 때문에 undefined가 됨
		if (document.formm.cseq.checked == true) {
			count++;
		}
	}
	// 두 개 이상 체크했을 경우
	for (var i=0; i<document.formm.cseq.length; i++) {
		if (document.formm.cseq[i].checked == true) {
			count++;
		}
	}
	// 하나도 체크하지 않았을 경우
	if (count == 0) {
		alert("삭제할 항목을 선택해 주세요.");
	}else{
		document.formm.action = "NonageServlet?command=cart_delete";
		document.formm.submit();
	}*/
	
	
	
	
	
}